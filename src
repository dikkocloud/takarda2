@tailwind base;
@tailwind components;
@tailwind utilities;

/* Custom, non-utility styles (tokens, motifs, components) live in
   custom.css and are already linked directly in every page —
   keep them separate so this file stays pure Tailwind. */
