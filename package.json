{
  "name": "takarda-website",
  "version": "1.0.0",
  "private": true,
  "description": "Takarda — a book club connecting Northern Nigeria's past, present and future.",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "build:css": "tailwindcss -i ./src/input.css -o ./css/tailwind-build.css --minify",
    "watch:css": "tailwindcss -i ./src/input.css -o ./css/tailwind-build.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4"
  }
}
